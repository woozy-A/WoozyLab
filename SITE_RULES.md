# WoozyLab 웹사이트 관리 규칙

이 문서는 WoozyLab 공식 웹사이트를 장기적으로 관리하기 위한 최상위 작업 규칙이다. 앱 소개, 고객지원, 개인정보 처리방침을 수정하거나 새 앱을 추가하기 전에 반드시 읽는다.

## 1. 단일 기준 정보

저장소 안의 다음 자료만 사이트 정보의 기준으로 사용한다.

1. `SITE_RULES.md`: 사이트 구조와 운영 규칙
2. `schema/app.schema.json`: 앱 데이터의 필수 구조와 타입
3. `apps/{slug}.json`: 앱 이름, 링크, 기능, 권한, 개인정보 사실 등 구조화된 앱 정보
4. `{slug}/index.html`, `{slug}/support/index.html`, `{slug}/privacy/index.html`: 실제 공개 문구
5. `assets/`: 실제로 배포되는 아이콘과 스크린샷

AI 대화 기억, 이전 대화 요약, 다른 저장소의 코드, 일반적인 플랫폼 관행은 앱 정보의 근거가 아니다. 구조화된 데이터와 공개 페이지가 다르면 배포를 중단하고 앱 소유자에게 확인한다.

## 2. WoozyLab 브랜드 원칙

- 공식 브랜드 표기는 `WoozyLab`로 통일한다.
- WoozyLab은 반복되는 귀찮음을 줄이는 작고 실용적인 앱을 소개한다.
- 앱마다 고유한 아이콘, 대표 색상, 제품 설명 방식은 유지한다.
- 공통 내비게이션, 푸터, 본문 폭, 접근성, 링크 상태는 WoozyLab 품질 기준을 따른다.
- 기존 사용자-facing 문구는 사실 오류나 명확한 관리 문제를 고치는 경우가 아니면 크게 바꾸지 않는다.
- 소개 페이지는 제품의 개성을 보여주고, 지원과 개인정보의 상세 내용은 전용 페이지를 기준으로 관리한다.
- 섹션 전체를 큰 카드 하나로 감싼 뒤 그 안에 카드를 반복 중첩하지 않는다.

## 3. URL과 파일 구조

앱 `slug`가 `example`이면 다음 공개 URL과 파일을 유지한다.

| 목적 | 공개 경로 | 저장소 파일 |
|---|---|---|
| 제품 소개 | `/example/` | `example/index.html` |
| 고객지원 | `/example/support/` | `example/support/index.html` |
| 개인정보 처리방침 | `/example/privacy/` | `example/privacy/index.html` |

루트 경로의 역할은 다음과 같다.

- `/`: WoozyLab, 출시 앱 목록, 독립 배포된 Web Labs 링크
- `/support/`: 앱을 선택하는 고객지원 허브
- `/privacy/`: 앱별 개인정보 처리방침을 선택하는 허브

한국어를 기본 경로로 유지하고 영어 페이지는 `/en/` 아래에 같은 역할로 추가한다.

| 목적 | 한국어 경로 | 영어 경로 |
|---|---|---|
| WoozyLab 홈 | `/` | `/en/` |
| 고객지원 허브 | `/support/` | `/en/support/` |
| 개인정보 허브 | `/privacy/` | `/en/privacy/` |
| 앱 소개 | `/{slug}/` | `/en/{slug}/` |
| 앱 고객지원 | `/{slug}/support/` | `/en/{slug}/support/` |
| 앱 개인정보 | `/{slug}/privacy/` | `/en/{slug}/privacy/` |

- 한국어와 영어의 대응 페이지는 서로 이동할 수 있는 언어 링크를 제공한다.
- `html` 요소의 `lang`과 언어 링크의 `hreflang`을 실제 페이지 언어와 일치시킨다.
- 영어 App Store 현지화에는 확인된 `/en/{slug}/`, `/en/{slug}/support/`, `/en/{slug}/privacy/` URL을 사용한다.
- 아직 영어 사실 검증이 끝나지 않은 앱은 영어 허브나 상세 페이지를 추측해 만들지 않는다.

이미 공개하거나 App Store에 등록한 URL은 삭제하거나 바꾸지 않는다. 경로를 옮겨야 한다면 기존 URL을 유지하는 호환 페이지 또는 리다이렉트를 먼저 마련하고 앱 소유자의 확인을 받는다. `/support/`와 `/privacy/`는 첫 KeyPic 등록에 사용된 호환 URL이므로 허브로 유지하되 KeyPic 전용 페이지로 가는 링크가 명확해야 한다. 다음 KeyPic 등록·업데이트부터는 전용 `/keypic/support/`와 `/keypic/privacy/`를 App Store Connect에 사용하는지 확인한다.

## 4. 공통 내비게이션과 푸터

모든 공개 페이지는 최소한 다음 이동 경로를 제공한다.

- WoozyLab 홈
- 현재 앱의 소개 페이지(앱별 페이지인 경우)
- 현재 앱의 지원 페이지(앱별 페이지인 경우)
- 현재 앱의 개인정보 처리방침(앱별 페이지인 경우)
- 루트 Support와 Privacy 허브

공통 푸터에는 `WoozyLab`, Support, Privacy 링크를 둔다. 지원 이메일은 앱 JSON의 `supportEmail`과 일치할 때만 표시한다. 내비게이션과 푸터의 공통 스타일은 `assets/css/site.css`에서 관리하고, 앱 고유 스타일은 별도 CSS 또는 페이지 테마 변수로 분리한다.

## 5. 앱 페이지 필수 정보

`apps/{slug}.json`은 `schema/app.schema.json`을 통과해야 한다. 각 제품 소개 페이지에는 저장소에서 확인된 범위 안에서 다음 정보를 제공한다.

- 앱 이름과 지원 플랫폼
- 한 줄 소개 또는 요약
- 주요 기능
- 실제 존재하는 앱 아이콘과 스크린샷
- 지원 페이지 링크
- 개인정보 처리방침 링크
- App Store 링크(실제 URL을 확인한 경우에만)

`updatedAt`은 해당 JSON을 저장소의 공개 페이지와 마지막으로 대조한 날짜다. 출시일이나 개인정보 처리방침 개정일을 뜻하지 않는다. 스키마가 `null`을 허용하는 필드에서 알 수 없는 값은 빈 문자열이나 임의 문구로 채우지 말고 `null`로 두며, 이유와 확인할 일을 `todos`에 기록한다. 앱 이름, 지원 URL, 개인정보 URL, 개인정보 사실처럼 스키마가 필수로 정한 사실은 추측해서 채우지 말고 앱 소유자의 확인이 끝날 때까지 배포하지 않는다.

## 6. 지원 페이지 작성 규칙

- 앱 이름과 지원 이메일은 해당 앱 JSON과 일치시킨다.
- 사용 방법, 문제 해결 절차, 필요한 OS 권한은 실제 앱 동작이나 앱 소유자의 확인 자료가 있을 때만 작성한다.
- 문의 시 필요한 정보(앱 버전, OS 버전 등)는 현재 지원 절차에 이미 있거나 앱 소유자가 승인한 경우에만 요구한다.
- GitHub Issues 등 외부 지원 채널은 실제로 운영 중인 URL만 연결한다.
- 제품 소개 페이지에는 짧은 지원 안내만 두고 상세 절차는 전용 지원 페이지에서 관리한다.

## 7. 개인정보 처리방침과 권한 규칙

> **경고:** 개인정보 수집 여부, 서버 전송, 분석 도구, 광고 추적, 외부 API, 계정, 보관 기간, 제3자 제공, OS 권한과 사용 목적은 절대 추측하지 않는다.

- 정책 문구는 현재 앱 동작과 앱 소유자가 확인한 사실만 사용한다.
- `기본 모드`, `무료 버전`, `우선` 같은 조건은 의미를 바꾸므로 임의로 삭제하거나 절대적인 표현으로 바꾸지 않는다.
- 권한 이름과 사용 목적은 `permissions`에 함께 기록한다. 정확한 권한 이름을 모르면 `null`과 TODO를 사용한다.
- 개인정보 사실은 `privacyFacts`에 기록하고 전용 개인정보 페이지와 같은 의미를 유지한다.
- 앱 동작이 바뀌면 코드 출시 전에 JSON, 개인정보 처리방침, 권한 안내, `updatedAt`을 같은 변경에서 검토한다.
- 법률 문구를 자동 생성하거나 다른 앱의 정책을 복사하지 않는다.

## 8. 새 앱 추가 절차

1. `apps/new-app.json`을 만들고 스키마의 모든 필드를 작성한다.
2. 확인되지 않은 값은 스키마가 허용할 때만 `null`로 두고 `todos`에 확인 항목을 적는다. 필수 사실이 확인되지 않았다면 배포를 중단한다.
3. 아이콘을 `assets/`에, 스크린샷을 `assets/screenshots/new-app/`에 추가하고 JSON 경로와 일치시킨다.
4. `new-app/index.html`, `new-app/support/index.html`, `new-app/privacy/index.html`을 만든다.
5. 루트 앱 목록, `/support/` 허브, `/privacy/` 허브에 새 앱 링크를 추가한다.
6. `npm run validate`로 JSON Schema, 내부 링크, 이미지 경로를 검사한다.
7. 데스크톱과 모바일 폭에서 세 페이지와 세 허브를 확인한다.
8. Git diff에서 앱 정보와 정책 문구를 다시 검토한다.
9. 검증이 모두 성공한 뒤 현재 배포 기준인 `main` 브랜치의 저장소 루트에 커밋하고 푸시한다.

현재 공개 루트 파일과 `origin/main`의 루트 `index.html`이 동일하고 저장소의 유일한 로컬 추적 브랜치가 `main`인 것을 확인했다. GitHub Pages 설정 API는 인증 없이 조회할 수 없으므로, 배포 설정을 바꾸는 경우에는 GitHub의 **Settings → Pages**에서 `main` / `(root)`인지 다시 확인한다.

독립 저장소에 배포한 웹 도구는 루트의 `Web Labs`에서 외부 링크로 소개할 수 있다. 이 경우 앱 JSON이나 App Store용 지원·개인정보 페이지를 억지로 만들지 않으며, 배포 전 실제 도구 URL의 HTTPS 응답과 화면 로딩을 확인한다.

## 9. 배포 전 검증 체크리스트

- [ ] 작업 전후 `git status --short --branch` 확인
- [ ] `npm run validate` 성공
- [ ] 모든 앱 JSON이 `schema/app.schema.json` 통과
- [ ] 앱 이름, Support URL, Privacy URL, `privacyFacts` 누락 없음
- [ ] 앱 JSON의 도메인·slug·페이지 역할과 실제 HTML의 핵심 정보가 일치
- [ ] 내부 링크와 이미지 경로 오류 없음
- [ ] 기존 공개 URL의 디렉터리와 `index.html` 유지
- [ ] 한국어·영어 대응 페이지의 상호 언어 링크와 `lang` 속성 확인
- [ ] 외부 링크와 `mailto:` 주소 수동 확인
- [ ] 데스크톱과 모바일에서 가로 스크롤, 잘림, 겹침 없음
- [ ] 키보드로 주요 링크에 접근 가능하고 포커스가 보임
- [ ] 아이콘의 대체 텍스트와 페이지 제목 확인
- [ ] 앱별 색상과 아이콘이 의도대로 유지됨
- [ ] 개인정보·권한·지원 정보에 추측이 들어가지 않음
- [ ] `git diff --check` 성공 및 변경 파일 범위 확인
- [ ] 푸시 후 기존 공개 URL, 영어 URL, 새 앱 URL이 HTTP 200인지 확인

## 10. AI 작업 시작 전 필수 읽기

AI가 사이트를 수정할 때는 다음 순서를 지킨다.

1. `git status --short --branch`
2. `SITE_RULES.md` 전체
3. `schema/app.schema.json`
4. 대상 `apps/{slug}.json`
5. 대상 앱의 소개·지원·개인정보 HTML
6. 루트 `index.html`, `support/index.html`, `privacy/index.html` 중 영향받는 파일

사이트 구조, URL, 공통 스타일, 배포 방식을 바꾸는 작업이면 `SITE_AUDIT.md`의 발견 문제와 남은 TODO도 읽는다. 이 점검 기록은 앱 사실의 원본을 대신하지 않는다.

정보가 없거나 서로 다르면 추측하여 작업을 계속하지 않는다. `null`과 TODO로 남기고 앱 소유자의 확인이 필요한 항목으로 보고한다.
